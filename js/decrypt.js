/* ==========================================================================
 *
 *  Use:
 *  "ENCRYPTED STR".decrypt()
 *  => Hello World!
 *
 * ========================================================================== */


String.prototype.decrypt = function () {
	return decrypted
};

module.exports = text => text.decrypt()
